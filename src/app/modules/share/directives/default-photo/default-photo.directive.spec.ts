import { DefaultPhotoDirective } from './default-photo.directive';

describe('DefaultPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultPhotoDirective();
    expect(directive).toBeTruthy();
  });
});
