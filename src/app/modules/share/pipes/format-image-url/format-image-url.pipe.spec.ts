import { FormatImageUrlPipe } from './format-image-url.pipe';

describe('FormatImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
